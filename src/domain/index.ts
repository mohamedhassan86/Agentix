/**
 * Domain layer entry point.
 * Pure TypeScript only — no framework, ORM, or UI imports.
 * This foundation intentionally contains no business aggregate;
 * it is an extension point for future specs.
 */
export const DOMAIN_LAYER_MARKER = "domain" as const;
