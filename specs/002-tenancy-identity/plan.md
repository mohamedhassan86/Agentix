# Implementation Plan: Tenancy & Identity

**Branch**: `002-tenancy-identity` | **Date**: 2026-09-12 | **Re-verified**: 2026-09-13 (plan workflow re-run; prerequisite state and spec defects confirmed) | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/002-tenancy-identity/spec.md`

**Note**: This template is filled in by the `/speckit-plan` command; its definition describes the execution workflow.

## Summary

Deliver the first identity and tenant boundary: email/password accounts, revocable server-side sessions, organizations, one role-bearing membership per user and organization, invitations and verification links, role enforcement, organization switching, logical deletion, identity events, and a read-only platform-inspection surface. Implement it as one Next.js/TypeScript vertical slice through the fixed Domain → Application → Infrastructure → App layers. PostgreSQL is the source of truth; Prisma repositories use a registry-driven tenant client that refuses organization-scoped work without a resolved member context or the explicitly read-only platform-inspection context. Organization ownership transfer is serialized and database-checked, and authorization always reloads current membership rather than trusting the session role snapshot.

Spec `001-solution-foundation` is **delivered** (verified 2026-09-13): all 46 tasks in `specs/001-solution-foundation/tasks.md` are complete and its implementation checklist (`specs/001-solution-foundation/checklists/implementation.md`) records the standard gates green — lint, 112 tests, build, license scan (994 packages), zero dependency-cruiser violations, and a validated additive migration `001_solution_foundation`. The foundation supplies the package scripts, composition roots, Problem Details/error middleware, telemetry baseline, design tokens, Prisma baseline (Prisma 7.10.x with `@prisma/adapter-pg` + native `pg`), transactional outbox and worker, and test harness this design consumes. Foundation concerns MUST NOT be re-scaffolded or absorbed into spec 002; 002 task generation and implementation can proceed against the delivered foundation.

## Technical Context

**Language/Version**: Node.js 22 LTS; TypeScript 5.x with `strict: true`

**Primary Dependencies**: Next.js 16.x, React 19.x, Auth.js 5.x Credentials provider and database adapter, Prisma Client 7.x, PostgreSQL driver/adapter selected by spec 001, Zod 4.x, `@asteasolutions/zod-to-openapi` 8.x, `argon2` 0.45.x, Lucide React; no paid service is required (all are permissive MIT/Apache-2.0/ISC dependencies; exact lockfile versions are selected at implementation time after the license scan)

**Storage**: PostgreSQL 16+ through Prisma on the delivered spec 001 baseline (`@prisma/adapter-pg` + native `pg`); shared schema; UUID primary keys; `timestamptz`; transactional outbox supplied by spec 001; no Redis dependency

**Testing**: Vitest for domain/application/UI tests, Testing Library for client interactions, Testcontainers with PostgreSQL 16 for repository/API/concurrency/isolation tests, Playwright for the identity golden path; OpenAPI 3.1 conformance and license scan in the standard gates

**Target Platform**: Next.js Node runtime on Vercel or equivalent TLS host; PostgreSQL managed service; same-origin modern browsers (Chrome/Edge/Firefox 111+, Safari 16.4+); Node worker for outbox delivery

**Project Type**: Full-stack web application in one Next.js codebase plus a worker, using the constitution's clean-architecture directories

**Performance Goals**: Non-LLM reads p95 < 300 ms at 10,000 identity rows per tenant; organization switch p95 < 300 ms server-side; sign-in intentionally dominated by password KDF; all paged collections default to 25 and cap at 100

**Constraints**: Fail closed on absent/invalid tenant context; foreign tenant produces zero payload/log leakage; database-backed revocable `HttpOnly`, `Secure`, `SameSite=Lax` sessions; sign-in begins without an active organization; roles are refreshed per request; exactly one Owner under concurrency; token/password material never logged or returned outside its one issued message; additive/reversible migration; no client-side direct database/authz logic; no real mail provider required

**Scale/Scope**: 5 user stories, 11 identity screens/states, 4 organization roles plus one non-membership platform privilege, 29 REST operations (counted from [contracts/openapi.yaml](./contracts/openapi.yaml)), 7 persisted identity concepts, 2 token lifecycles, and automated coverage of every in-scope permission-matrix cell and organization-scoped operation

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Pre-research gate

| Principle / constraint | Status | Plan evidence |
| --- | --- | --- |
| I. Spec-driven delivery | ✅ PASS | Work remains in `specs/002-tenancy-identity`; clarify was run and recorded in the spec. Tasks and code are not generated by this command. Spec 001 is delivered (46/46 tasks, gates green per its implementation checklist) and 002 builds on it without re-scaffolding foundation concerns or absorbing hidden scope. |
| II. Clean architecture | ✅ PASS | Domain has no runtime dependencies; Application owns use cases/ports/policies; Infrastructure owns Prisma/Auth.js/Argon2/outbox adapters; App owns parsing, dispatch, pages, and composition. |
| III. Rich domain model | ✅ PASS | Organization/Membership and Invitation transitions and invariants are specified in [data-model.md](./data-model.md); ownership transfer is behavior, not generic update. |
| IV. Thin route handlers | ✅ PASS | Contract operations map to one parse plus one command/query dispatch. Auth, tenant resolution, current-role lookup, correlation, and Problem Details mapping are middleware/pipeline behavior. |
| V. Tenant isolation | ✅ PASS | Registry-driven scoped Prisma client, deny-by-default tenant context, fetch-under-filter writes, no request org override except switch, named cross-tenant tests for every scoped operation. Platform inspection is a separate read-only context and repository surface, never a general filter bypass. |
| VI. Secret handling | ✅ PASS | Passwords use Argon2id; opaque token digests are stored; raw token material is only present in the issued message path and encrypted while awaiting outbox delivery; redaction tests cover logs, responses, events, and OpenAPI. |
| VII. Verified ingress only approval | ✅ PASS | No run or approval behavior and no approval route is introduced. |
| VIII. Source provider neutrality | ✅ N/A | No source-host integration exists in this feature. |
| IX. Hooks/metering | ✅ N/A | No LLM/provider call exists. Identity lifecycle facts use the foundation outbox but do not create an agent bus. |
| X. Test-gated DoD | ✅ PASS | Quickstart requires lint, unit/integration/UI/E2E tests, build/type-check, migration check, OpenAPI conformance, and license scan. Concurrency, matrix, isolation, and redaction suites are named. |
| XI. Design fidelity | ✅ PASS | UI reuses the canonical tokens/components and records all required Design Deltas below; loading, empty, error, denied, keyboard, focus, dialog, and reduced-motion behavior are included. |
| Global runtime/data/API | ✅ PASS | Node 22, strict TS, Next 16/React 19, PostgreSQL 16, Prisma, `/api/v1`, OpenAPI 3.1, RFC 9457, cursor pagination, UUIDs, UTC timestamps. |
| Dependency licensing | ✅ PASS | Proposed runtime/dev dependencies are MIT, Apache-2.0, or ISC; implementation must pin them and run the repository license gate before acceptance. |
| Delivery ordering | ✅ PASS | Verified 2026-09-13: spec 001 is delivered (all 46 tasks complete; gates green per `specs/001-solution-foundation/checklists/implementation.md`). The prerequisite that gated this plan is satisfied; `/speckit-tasks` proceeds, and implementation must not scaffold foundation concerns here. |

No constitution exception is requested. The former prerequisite warning is cleared: spec 001 is delivered, so no stop condition remains before task generation or implementation.

### Post-design re-check

Phase 1 artifacts preserve the gate:

- [data-model.md](./data-model.md) makes tenant ownership explicit on every scoped record and uses database constraints plus serialized aggregate operations for ownership.
- [contracts/openapi.yaml](./contracts/openapi.yaml) has no generic organization-id override on member-scoped routes; only organization switch and the separately authorized platform inspection route name a target organization. Owner operations on an inactive organization are performed by first switching to it, resolving FR-034 conservatively.
- [contracts/events.md](./contracts/events.md) separates redacted identity facts from encrypted message-delivery payloads.
- [quickstart.md](./quickstart.md) includes per-operation foreign-tenant, current-role, token-redaction, concurrent transfer, and design/accessibility checks.
- No unresolved `NEEDS CLARIFICATION` remains. The spec's “delete an organization that is not active” edge case is satisfied as a two-step switch-then-delete flow so tenant identity is never accepted as an override on a scoped mutation.

### UI: Design Delta

The canonical `Public/Desgin/index.html` remains the visual and interaction source. These deliberate behavior/content changes are required by the feature spec:

| Mock behavior | Planned behavior | Reason / governing rule |
| --- | --- | --- |
| Product name “SpecOps” | “Agentix” | Spec assumption; no new visual language. |
| Sign-up includes organization, region, and seats | Account fields first; create-organization is the next state/dialog; no region/seats | FR-040/042 and out-of-scope residency/billing. Still at most three screens. |
| Sign-in includes organization picker and may restore one | Email/password only, followed by mandatory choose-organization or zero-org empty state | FR-005/036. |
| GitHub and SAML sign-in buttons | Omitted or disabled with “Not available in this release” reason | SSO/social login are out of scope; FR-041 forbids silent capability ambiguity. |
| Invite role includes Owner and project access | Roles are Admin/Member/Viewer only; no project access field | FR-017/026 and project scope is deferred. |
| Viewer cost visibility differs in mock | Keep identity-only Viewer read behavior; later cost UI must follow spec's reserved matrix | Feature scope and spec precedence. |
| Settings includes region, domain, trigger, branch, budget, retention, secret posture | Only identity profile (name/slug), membership controls, transfer, and logical delete are active; deferred controls are not wired | FR-040 and Out of Scope. |
| Mock does not draw all states | Add loading, empty, error, permission-denied, expired/revoked invite, unverified/already-verified, and no-active-org states | Principle XI and FR-041. |

All retained components use the canonical CSS custom properties and inventory (`card`, `segmented`, `field`, `status-chip`, `banner`, `table-wrap`, `modal`, `toast-stack`). Every modal/menu traps focus, closes on Escape, restores trigger focus, has labels and inline errors, and respects reduced motion.

## Project Structure

### Documentation (this feature)

```text
specs/002-tenancy-identity/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── openapi.yaml
│   └── events.md
├── checklists/
│   └── requirements.md
└── tasks.md              # Created by /speckit-tasks, not this command
```

### Source Code (repository root)

```text
prisma/
├── schema.prisma
└── migrations/
    └── *_002_tenancy_identity/

src/
├── domain/
│   └── identity/
│       ├── entities/
│       ├── value-objects/
│       ├── events/
│       ├── policies/
│       └── errors/
├── application/
│   └── identity/
│       ├── commands/
│       ├── queries/
│       ├── handlers/
│       ├── dto/
│       ├── policies/
│       └── ports/
├── infrastructure/
│   └── identity/
│       ├── auth/
│       ├── persistence/
│       ├── tenancy/
│       ├── messaging/
│       └── telemetry/
├── app/
│   ├── (auth)/
│   ├── (identity)/
│   ├── api/v1/
│   ├── components/identity/
│   └── lib/api/
└── worker/
    └── identity/

tests/
├── unit/
│   ├── domain/identity/
│   ├── application/identity/
│   └── ui/identity/
├── integration/
│   ├── api/identity/
│   ├── persistence/identity/
│   └── tenancy/
├── contract/
│   └── identity/
└── e2e/
    └── identity.spec.ts
```

**Structure Decision**: Use the constitution's single full-stack Next.js repository with feature-cohesive `identity` modules inside each fixed architectural layer. Prisma remains infrastructure-only. The UI and `/api/v1` routes live in `src/app`; the existing worker dispatches message outbox records. Tests mirror architectural boundaries and keep security suites easy to locate. If spec 001 chooses equivalent concrete subpaths, 002 follows those paths without changing dependency direction and updates this tree before task generation.

## Complexity Tracking

No constitution violation is accepted. Two security mechanisms warrant explicit review even though both implement binding principles:

| Mechanism | Why Needed | Simpler Alternative Rejected Because |
| --- | --- | --- |
| Registry-driven tenant-scoped Prisma client plus separate read-only platform-inspection client | Principle V requires a new tenant model to fail closed automatically, while FR-035 requires a narrow cross-tenant read capability. The registry asserts all scoped models are covered and the inspection client exposes queries only. | Ad hoc `org_id` predicates are easy to omit; one unrestricted Prisma client in Application/App would turn platform inspection into a mutation-capable bypass. |
| Encrypted transient outbox payload for verification/invitation delivery | The transaction must persist a durable delivery request even when the provider is down, but raw single-use tokens must not be stored in normal rows, events, logs, or responses. Use Node's standard AES-256-GCM with a deployment message-delivery key from typed server-only config; persist only ciphertext/nonce/tag until terminal dispatch, then purge token-bearing payload. | Plaintext outbox payload leaks bearer tokens; synchronous email creates a dual-write/rollback problem; waiting for spec 003 would block this spec's required links. This is not the organization secret vault and introduces no crypto dependency. |
